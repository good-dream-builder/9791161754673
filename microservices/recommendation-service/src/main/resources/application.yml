server:
  port: 7002
  error:
    include-message: always

spring:
  application:
    name: recommendation-service

logging:
  level:
    root: INFO
    com.songko: DEBUG

---
spring:
  config:
    activate:
      on-profile: docker
server:
  port: 8080